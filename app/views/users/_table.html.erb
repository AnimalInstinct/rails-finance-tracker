<table class="table table-hover mt-5">
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
        <tr>
            <td><%= user.full_name %></td>
            <td>
                <!-- <%= link_to (fa_icon 'trash'), users_path(user), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-sm btn-danger' %> -->
            </td>
        </tr>
        <% end %>
    </tbody>
</table>